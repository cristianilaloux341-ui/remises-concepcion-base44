{
  "description": "",
  "name": "Deep Dispatch Reconciler",
  "definition": {
    "document": {
      "dsl": "1.0.0",
      "name": "deep_dispatch_reconciler",
      "version": "1.0",
      "namespace": "base44"
    },
    "do": [
      {
        "run_function": {
          "call": "invoke_backend_function",
          "with": {
            "function_name": "dispatchPendingOnDriverQueueEntry",
            "args": {}
          },
          "then": "end"
        }
      }
    ]
  },
  "trigger": {
    "condition": "${ ((.trigger.changed_fields | index(\"queue_entered_at\") != null) or (.trigger.changed_fields | index(\"dispatch_status\") != null) or (.trigger.changed_fields | index(\"reserved_order_id\") != null) or (.trigger.changed_fields | index(\"reservation_token\") != null) or (.trigger.changed_fields | index(\"active_order_id\") != null) or (.trigger.changed_fields | index(\"active_ride_id\") != null) or (.trigger.changed_fields | index(\"status\") != null) or (.trigger.changed_fields | index(\"current_base\") != null)) }",
    "config": {
      "trigger_type": "entity",
      "events": [
        "update"
      ],
      "entity_name": "Driver"
    }
  },
  "x-base44-migrated-from-automation": {
    "legacy_payload_function": "dispatchReconciler"
  }
}