{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "operador"
      ],
      "default": "operador",
      "description": "Rol del usuario: admin (directivo) u operador"
    }
  }
}