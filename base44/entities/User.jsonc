{
  "name": "User",
  "type": "object",
  "properties": {
    "role": {
      "type": "string",
      "enum": [
        "admin",
        "user"
      ],
      "default": "user"
    },
    "push_subscription": {
      "type": "string",
      "description": "JSON serializado de la PushSubscription del navegador (operadores)"
    }
  }
}