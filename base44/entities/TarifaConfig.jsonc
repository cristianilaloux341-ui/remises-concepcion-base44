{
  "name": "TarifaConfig",
  "type": "object",
  "properties": {
    "bajada_bandera": {
      "type": "number",
      "default": 500,
      "description": "Importe fijo al iniciar un viaje"
    },
    "precio_por_km": {
      "type": "number",
      "default": 2000,
      "description": "Precio por kil\u00f3metro recorrido"
    },
    "precio_por_minuto_corrido": {
      "type": "number",
      "default": 30,
      "description": "Precio por minuto de viaje en movimiento"
    },
    "precio_por_minuto_espera": {
      "type": "number",
      "default": 50,
      "description": "Precio por minuto de espera (velocidad < 5 km/h)"
    },
    "tolerancia_espera_segundos": {
      "type": "number",
      "default": 120,
      "description": "Segundos de gracia antes de cobrar espera"
    }
  },
  "required": []
}